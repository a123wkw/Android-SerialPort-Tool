# ======================
# 项目级Gradle配置
# ======================

# IDE配置优先级警告
# > 通过Android Studio等IDE设置的配置会覆盖此文件

# AndroidX配置
android.enableJetifier=true  # 启用Jetifier转换工具
android.useAndroidX=true     # 使用AndroidX库

# JVM内存配置
org.gradle.jvmargs=-Xmx1536m  # 分配1.5GB堆内存

# 并行构建(实验性)
# > 仅适用于解耦项目
# org.gradle.parallel=true
